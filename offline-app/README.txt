Free Tinnitus Help v4.2 installable PWA.
Upload all files together to an HTTPS host. Android/Chrome can show Install App when eligible.
On iPhone, installation is via Safari > Share > Add to Home Screen.
Offline core files are precached.
Background/lock-screen Web Audio still requires physical-device validation; native audio is the fallback if the OS suspends it.
